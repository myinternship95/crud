<%= erb :nav %>
<section class="intro-section">
  <div class="container">
    <h1><%= @book.title %></h1>
    <h1>Authors: $<%= @book.authors %></h1>
    <h1>Publisher: $<%= @book.publisher %></h1>
    <h1>Year: $<%= @book.year %></h1>
<ul>
</ul>
<button class="btn btn-default"><a href="/books/<%= @book.id %>/edit">Edit Book</a></button>

<form action="/books/<%=@book.id %>/delete" method="post">
  <input type="hidden" id="hidden" name="_method" value="delete">
  <input type="submit" class="btn btn-danger" value="delete book">
</form>
</div>
</section>
